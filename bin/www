#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');

app.listen(3000, function() {
    console.log('Server listening on port %d', 3000);
});

// test: curl -i http://localhost:3000/
